# Square root of a number
print(sqrt(5))
cat('\n')

# pi is spl variable
v=log10(200)*cos(pi)
print(v)
cat('\n')

# generate random number

rand_num = sample(1:100,1)
print(rand_num)
cat('\n')

# generate random number with n number of digits

n = 3
round_num = round(runif(2,0,100),n) # runif also generates random number
print(round_num)
cat('\n')
